# AGENTS.md

## الهدف

هذا المستودع يبني وكيل برمجي محلياً داخل Termux. الأولوية هي **السلامة، قابلية التراجع، والعمل دون اتصال**؛ لا تُضاف أدوات واسعة الصلاحيات لمجرد الراحة.

## قواعد التنفيذ

1. ابدأ دائماً بوضع `plan` عند استكشاف مساحة العمل أو تحليل مهمة جديدة.
2. لا تنتقل إلى `build` إلا بعد تحديد الجذر، عرض الأوامر، والتحقق من قائمة السماح.
3. لا تستخدم مسارات مطلقة خارج جذر مساحة العمل، ولا تتبع الروابط الرمزية عند الكتابة.
4. لا تُرسل أسراراً أو محتوى ملفات إلى مزود نموذج لغوي من دون إعداد صريح من المستخدم.
5. استخدم SQLite للجلسات المحلية وGit للقطات والتراجع؛ لا تحذف سجل المستخدم.
6. التزم بأدوات قليلة عالية الإشارة، وفضّل `search_text` على إعادة ملفات ضخمة كاملة.
7. اختبر أي تغيير عبر `python -m pytest` قبل إنشاء commit.
8. يجب أن تكون سير عمل GitHub قابلة للفحص، وبأقل صلاحيات ممكنة، ولا تُنفذ أوامر غير موثوقة من نص القضايا أو طلبات السحب.

## أوامر Termux

- تثبيت الاعتماديات: `pkg install -y python git ripgrep proot-distro termux-api`
- الوصول إلى التخزين المشترك: `termux-setup-storage`
- بيئة glibc المعزولة: `proot-distro login debian`
- إشعار المستخدم: `termux-notification`

## أسلوب الكود

يجب أن يعمل الكود على Python 3.10 أو أحدث، وأن يستخدم المكتبة القياسية قدر الإمكان حتى يعمل دون شبكة. يجب أن تكون الأخطاء واضحة، وأن تكون مخرجات CLI قابلة للقراءة البشرية وقابلة للتحليل الآلي عند الحاجة.
